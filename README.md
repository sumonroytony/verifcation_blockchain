# verifcation_blockchain

#Installation
--npm install
--cd frontend
--npm install

#Setup
--create a .env file in the root directory and set PORT=5000

#Start app
--go to root directory and run npm run dev
